{
  "name": "blackducksecuritycheck",
  "version": "1.0.0",
  "description": "Uses the service connection established for Black Duck task to perform additional API calls to determine if any CRITICAL or HIGH security risks were introduced into the product. If a critical or high security error is detected it will fail the build.",
  "main": "index.js",
  "scripts": {
    "install": "cd ./bdscan && npm install",
    "test": "cd ./bdscan/tests && tsc && jest -- index.test.js",
    "build": "cd ./bdscan && npm run build",
    "package": "tfx extension create --manifest-globs vss-extension.json",
    "create": "npm install && npm run build && npm run package"
  },
  "repository": {
    "type": "git",
    "url": "git@ssh.dev.azure.com:v3/allegionit/ProductTech/blackduck-pipeline-security-status"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
